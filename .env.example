# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# Opt-in flag to enable the worldcup2026 fixture sync (see lib/worldcup-sync.js).
# Must be the exact string "true" to enable; anything else (or unset) keeps sync disabled.
WORLDCUP_SYNC_ENABLED=true
